.skill-cards {
  width: 100%;
  padding: 20px;
  margin: 20px;
}
.skill-item-list {
  display: inline-flex;
  flex-flow: row wrap;
}
.skill-item {
  width: 17rem;
  border: solid .1rem $basecolor;
  border-radius: .2rem;
  padding: 1rem;
  margin: 1rem;
}
.skill-item-top {
  display: flex;
}
.skill-item-label {
  
}    
.skill-item-term {
  color: gray;
  margin: 0 auto;
  margin-right: 1rem;
}